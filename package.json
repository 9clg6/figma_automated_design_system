{
  "name": "figma-automated-design-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Nightly Figma DS sync → Dart code via Claude AI",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "extract": "node scripts/extract-figma.mjs",
    "sync": "node scripts/figma-to-dart.mjs",
    "dry-run": "echo 'Dry run — would call figma-to-dart.mjs'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  }
}
